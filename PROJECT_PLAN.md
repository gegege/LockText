# 📘 LockText 開発方針・構成計画

---

## 🎯 プロジェクトの目的

LockText は「すべての処理をクライアントサイドで完結させる」ことにより、  
サーバーへのデータ送信リスクを一切排除した、**自己完結型のセキュアなテキスト暗号化ツール**です。

---

## 📅 バージョン戦略

### 📌 採用形式：日付ベースバージョン

- フォーマット：`vX.YYMMDD`（例：`v2.250601`）
- バージョン番号はUIや暗号フォーマットの互換を表す目安として利用
- 暗号ファイルにも `version` メタ情報を埋め込み予定

---

## 🧩 ブランチ戦略

| ブランチ名        | 用途                        | 備考 |
|-------------------|-----------------------------|------|
| `legacy/v1`        | v1（旧UI）凍結・保守用       | `docs/v1/` に対応 |
| `release/v2`       | v2 安定版公開ブランチ        | `docs/v2/` に対応 |
| `develop/v2`       | v2 の機能追加・統合ブランチ  | PRはここに向ける |
| `feature/v2-*`     | v2関連の個別機能開発         | 機能単位で命名 |
| `hotfix/v2-*`      | 緊急修正など                 | セキュリティ・リグレッション用 |

> 将来は `develop/v3`, `release/v3`, `legacy/v2` に発展可能

---

## 🛠 CI/CD構成（GitHub Actions）

### GitHub Pages への公開（`docs/` ディレクトリ）

- `develop/v2` → `npm run build` → `docs/v2/` へ出力
- `legacy/v1` の `docs/v1/` は静的に固定保存
- `docs/index.html` は `v2/` にリダイレクト、または選択画面を表示

### ワークフロー例：
```yaml
on:
  push:
    branches:
      - develop/v2
